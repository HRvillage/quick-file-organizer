# Quick File Organizer v1.1.0

## Highlights

- Added grouped naming assistant for repeated file sets.
- Added flexible filename format presets.
- Added broader file type support for audio, advanced images, design files, CAD drawings, and 3D models.
- Added Any mode for renaming all non-hidden, non-system files when the extension is not in the preset list.
- Added time gap warnings for grouped naming previews.
- Added confirmation when a batch includes multiple file extensions.

## New File Type Support

- Audio: MP3, WAV, OGG, WMA, AAC
- Advanced images: HEIC, TIFF, RAW formats
- Design files: PSD, AI, INDD, Sketch, FIG
- CAD drawings: DWG, DWF, DXF
- 3D models: STEP, IGES, STL, 3DS

## Notes

Quick File Organizer still works locally and does not upload files or filenames. Settings and undo history remain stored in the local `Data` folder.

Download the portable ZIP, extract it, and run `QuickFileOrganizer.exe`.

---

# Quick File Organizer v1.1.0

## 更新重點

- 新增分組命名助手，適合固定數量、重複項目的檔案集合。
- 新增常用檔名格式，可選擇日期、名稱與流水號的排列方式。
- 新增更多檔案類型支援，包含音訊、進階圖片、設計檔、CAD 圖面與 3D 模型。
- 新增 Any 模式，可在副檔名不在預設清單時納入資料夾內所有非隱藏、非系統檔案。
- 分組命名預覽新增時間間隔提醒，協助檢查漏拍、混入無關檔案或排序錯誤。
- 同一批次包含多種副檔名時，會先提醒確認。

## 新增檔案類型

- 音訊：MP3、WAV、OGG、WMA、AAC
- 進階圖片：HEIC、TIFF、RAW 格式
- 設計檔：PSD、AI、INDD、Sketch、FIG
- CAD 圖面：DWG、DWF、DXF
- 3D 模型：STEP、IGES、STL、3DS

## 備註

Quick File Organizer 仍然只在本機執行，不會上傳檔案或檔名。設定與還原紀錄會保存在本機 `Data` 資料夾。

下載 portable ZIP，解壓縮後執行 `QuickFileOrganizer.exe` 即可。
